{
  "name": "pandoc-ssg",
  "version": "0.1.0",
  "description": "Pandoc-centered static site generator: a small kernel for scanning, validation, routing, and manifest generation around pandoc-owned documents",
  "type": "module",
  "bin": {
    "pandoc-ssg": "src/cli.ts"
  },
  "files": [
    "src",
    "pandoc"
  ],
  "scripts": {
    "test": "bun test --coverage --coverage-reporter=lcov --coverage-dir=.",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "smol-toml": "^1.3.1",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
